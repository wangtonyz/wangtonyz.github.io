#!/bin/sh
# pre-push hook: push 前的最后安全检查
# 注意: commit-msg hook 已通过 commitlint 验证消息格式

echo "🔍 push 前安全检查..."

# 简单检查本地是否有未提交的改动
if [ -n "$(git status --porcelain)" ]; then
  echo "⚠️ 工作区有未提交的改动，请先提交"
  git status
  exit 1
fi

echo "✅ push 前检查完成"
exit 0
